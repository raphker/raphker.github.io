<!DOCTYPE html>
<html lang="{{ page.lang }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Raph</title>
        <link rel="stylesheet" href="/styles/index.css">
    </head>
    <body>
        <header>
            <div class="wrapper" data-type="wide">
                <a href="/" class="header_title">{{ config.title }}</a>
                <nav class="nav--desktop">
                    <ul role="list">
                        {% for item in collections.page | favor_lang | sort_by_priority%}
                            <li>
                                <a href="{{ item.url }}" aria-current={{"page" if page.url == item.url or (item.data.collection and tags in item.data.collection) else "false" }}>{{item.data.title}}</a>
                            </li>
                        {% endfor %}
                    </ul>
                </nav>
                <nav class="nav--mobile">
                    <c-hamburger>
                        <button aria-expanded="false">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" width="1em" height="1em">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                            </svg>
                        </button>
                        <ul role="list">
                            {% for item in collections.page | favor_lang | sort_by_priority%}
                                <li>
                                    <a href="{{ item.url }}" aria-current={{"page" if page.url == item.url or (item.data.collection and tags in item.data.collection) else "false" }}>{{item.data.title}}</a>
                                </li>
                            {% endfor %}
                        </ul>
                    </c-hamburger>
                </nav>

            </div>
        </header>
        <svg viewBox="0 0 300 10" xmlns="http://www.w3.org/2000/svg" class="decorator">
            <path d="M0 0 V5 H300 V-5 Z" class="decorator_shape"/>
            <path d="M0 5 300 5"/>
        </svg>

        <main class="wrapper flow">
            {% block content %}
            {% endblock content %}
        </main>
        <footer class="footer wrapper" data-type="wide">
            <p class="footer_copyright">{{config.copyright}}</p>
            {% if page.url | locale_links | length %}
                <ul role="list" class="footer_languages">
                    {% for link in page.url | locale_links %}
                        {%- if not loop.first %},{% endif %}
                        <li>
                            <a href="{{link.url}}" lang="{{link.lang}}" hreflang="{{link.lang}}">{{link.label}}</a>
                        </li>
                    {% endfor %}
                </ul>
            {% endif %}
        </footer>
    </body>

    <script src="/scripts/index.js"></script>
</html>