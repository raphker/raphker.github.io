<!DOCTYPE html>
<html lang="{{ page.lang }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Raph</title>
        <link rel="stylesheet" href="/styles/index.css">
    </head>
    <body>
        <header>
            This page is also available in:
            {% for link in page.url | locale_links %}
                {%- if not loop.first %},{% endif %}
                <a href="{{link.url}}" lang="{{link.lang}}" hreflang="{{link.lang}}">{{link.label}}</a>
            {% endfor %}
        </header>
        <nav>
            <ul>
                {% for item in collections.page | favor_lang %}
                    <li>
                        <a href="{{ item.url }}" aria-current={{"page" if page.url == item.url or current == item.fileSlug else "false" }}>{{item.data.title}} </a>
                    </li>
                {% endfor %}
            </ul>
        </nav>


        {{ content | safe }}
    </body>
    <script src="/scripts/index.js"></script>
</html>