{% import "utils.njk" as utils%}
{%extends "layouts/base.njk"%}

{% set isHome = true %}

{% block content %}
    <div class="projects">
    <ul role="list" class="projects_list region" style="--region-space:var(--space-3xl)">
        {% for project in collections.projects | favor_lang | sort_by_priority %}
            <li>
                <a href="{{ project.url }}" class="projectCard">
                    {% image project.data.thumbnail.src, project.data.thumbnail.alt, ["auto"], undefined, true, ["webp","png"] %}
                    <span class="projectCard_title bold text-xl">{{ utils.splitLetters(project.data.title) }}</span>
                </a>
            </li>
        {% endfor %}
        {% for project in collections.projects | favor_lang  %}
            <li>
                <a href="{{ project.url }}" class="projectCard">
                    {% image project.data.thumbnail.src, project.data.thumbnail.alt, ["auto"], undefined, true, ["webp","png"] %}
                    <span class="projectCard_title bold text-xl">{{ utils.splitLetters(project.data.title) }}</span>
                </a>
            </li>
        {% endfor %}
    </ul>
    </div>

{% endblock content %}
